% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_thesis.R
\name{create_thesis}
\alias{create_thesis}
\title{Scaffold a new uomthesis project}
\usage{
create_thesis(
  path,
  format = c("standard", "journal"),
  degree = "PhD",
  faculty = "Humanities",
  school = "Alliance Manchester Business School",
  division = NULL,
  author = NULL,
  title = NULL,
  year = NULL,
  reference_style = c("harvard-manchester", "apa", "chicago-author-date", "mhra",
    "vancouver"),
  engine = c("lualatex", "xelatex", "pdflatex"),
  mainfont = "Times New Roman",
  list_of_publications = NULL,
  num_papers = 3L,
  force = FALSE,
  open = rlang::is_interactive()
)
}
\arguments{
\item{path}{Path to the new project directory.}

\item{format}{\code{"standard"} or \code{"journal"}.}

\item{degree}{One of the degrees in the policy allowed set.}

\item{faculty}{Faculty name. Default: \code{"Humanities"}.}

\item{school}{School name. Default: \code{"Alliance Manchester Business School"}.}

\item{division}{AMBS division code: \code{"A&F"} | \code{"IMP"} | \code{"MSM"} | \code{"PMO"}, or \code{NULL}.}

\item{author}{Named list with components \code{forename}, \code{middle_initial},
\code{surname}, \code{native_name} (optional). If \code{NULL}, placeholders are used.}

\item{title}{Thesis title. If \code{NULL}, a placeholder is used.}

\item{year}{Year of submission (integer). Defaults to current year.}

\item{reference_style}{One of \code{list_csl()$name}.}

\item{engine}{\code{"lualatex"} | \code{"xelatex"} | \code{"pdflatex"}.}

\item{mainfont}{One of the policy-allowed fonts.}

\item{list_of_publications}{For journal format: if \code{TRUE} (default for
journal), a \code{chapters/00-publications.qmd} page is scaffolded and listed
in \verb{_quarto.yml}. Set to \code{FALSE} if you have no papers to list yet --
the page is omitted from both the directory and the book chapters. Has
no effect for \code{format = "standard"}.}

\item{num_papers}{For journal format: integer 1-10, the number of
constituent paper chapters to scaffold (Introduction + N papers +
Conclusion). Default \code{3} (the typical AMBS journal-format thesis size
per policy section 13.5). Paper 1 is always the rich example chapter
that demonstrates citations, equations, tables, figures, and per-paper
appendices; Papers 2..N are bare structural templates with letter-
prefixed appendix headings (N.A, N.B). Has no effect for
\code{format = "standard"}.}

\item{force}{If \code{TRUE}, allow \code{path} to exist as long as it is empty.}

\item{open}{If \code{TRUE} and RStudio is available, open the new project.}
}
\value{
Invisible path to the created project.
}
\description{
Creates a directory tree containing a Quarto project (\verb{_quarto.yml},
\code{index.qmd}, chapter stubs), copies the appropriate Quarto extension
(standard or journal) and a chosen CSL file into the project's
\verb{_extensions/} directory, and substitutes user-supplied metadata
into the scaffolded \code{index.qmd}.
}
