# Parse the YAML front-matter of a .qmd file

Parse the YAML front-matter of a .qmd file

## Usage

``` r
parse_qmd_yaml(path)
```

## Arguments

- path:

  Path to a .qmd file.

## Value

A list, possibly empty.
