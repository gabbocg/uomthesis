# Constants drawn from the University of Manchester Presentation of Theses Policy

Single source of truth for every policy-derived constant used elsewhere
in the package. Each entry carries a comment with its policy section
number so future policy revisions can be applied surgically.

## Usage

``` r
policy_constants()
```

## Value

A named list.
