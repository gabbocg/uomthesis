# Render a check report

Render a check report

## Usage

``` r
render_check_report(report, format = c("console", "markdown", "json"))
```

## Arguments

- report:

  A `uomthesis_check_report`.

- format:

  "console" \| "markdown" \| "json".

## Value

Invisible character (markdown/json) or report (console).
