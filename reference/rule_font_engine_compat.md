# Rule: mainfont must be pdflatex-safe when pdf-engine is pdflatex

Rule: mainfont must be pdflatex-safe when pdf-engine is pdflatex

## Usage

``` r
rule_font_engine_compat()
```

## Value

A rule spec list.
