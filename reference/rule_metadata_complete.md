# Rule: all required metadata fields must be present and non-empty

Rule: all required metadata fields must be present and non-empty

## Usage

``` r
rule_metadata_complete()
```

## Value

A rule spec list.
