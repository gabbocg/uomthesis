# Count words in body text, stripping YAML, code chunks, and equations

Count words in body text, stripping YAML, code chunks, and equations

## Usage

``` r
word_count_text(lines)
```

## Arguments

- lines:

  Character vector of lines.

## Value

Integer count.
